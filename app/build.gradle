apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'oDpe-1726'
            keyPassword 'oDpe-1726'
            storeFile file('/home/dimprount/AndroidStudioProjects/Myapks/levelercamera.jks')
            storePassword 'oDpe-1726'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.2"
    defaultConfig {
        applicationId "com.dimiprount.levelercamera"
        minSdkVersion 15
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            pseudoLocalesEnabled true
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.1.1'
}
